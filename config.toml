# VoipGlot Windows Audio Translation Configuration

[audio]
# Audio device settings
input_device = null  # Set to specific device name or leave null for default
output_device = null  # Set to specific device name or leave null for default
sample_rate = 16000   # Sample rate in Hz
channels = 1          # Number of audio channels (1 = mono, 2 = stereo)
buffer_size = 1024    # Audio buffer size in samples
latency_ms = 50       # Target latency in milliseconds

[translation]
# Translation settings
source_language = "en"  # Source language code
target_language = "es"  # Target language code
stt_provider = "Whisper"  # Speech-to-Text provider: Whisper, Azure, Google
translation_provider = "DeepL"  # Translation provider: DeepL, Google, Azure
tts_provider = "Azure"  # Text-to-Speech provider: Azure, ElevenLabs, Google

[api]
# API keys (set via environment variables for security)
deepl_api_key = null  # Set VOIPGLOT_DEEPL_API_KEY environment variable
azure_speech_key = null  # Set VOIPGLOT_AZURE_SPEECH_KEY environment variable
azure_region = null  # Set VOIPGLOT_AZURE_REGION environment variable
elevenlabs_api_key = null  # Set VOIPGLOT_ELEVENLABS_API_KEY environment variable
google_api_key = null  # Set VOIPGLOT_GOOGLE_API_KEY environment variable

[processing]
# Audio processing settings
chunk_duration_ms = 1000  # Duration of audio chunks to process
silence_threshold = 0.01  # Threshold for silence detection
noise_reduction = true    # Enable noise reduction
echo_cancellation = true  # Enable echo cancellation 